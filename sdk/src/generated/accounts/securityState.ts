/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getHaltStateDecoder,
  getHaltStateEncoder,
  getObservationDecoder,
  getObservationEncoder,
  getOracleBindingDecoder,
  getOracleBindingEncoder,
  type HaltState,
  type HaltStateArgs,
  type Observation,
  type ObservationArgs,
  type OracleBinding,
  type OracleBindingArgs,
} from "../types/index.js";

export const SECURITY_STATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  61, 133, 136, 156, 159, 96, 12, 57,
]);

export function getSecurityStateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SECURITY_STATE_DISCRIMINATOR,
  );
}

export type SecurityState = {
  discriminator: ReadonlyUint8Array;
  /** The Token-2022 mint this security *is*. Also its PDA seed. */
  underlyingMint: Address;
  /** ASCII, right-padded with zeroes: "AAPLx". */
  symbol: ReadonlyUint8Array;
  /** Which committed exchange calendar gates it. */
  calendarId: number;
  /** Copied from the mint so a reader does not have to unpack it. */
  decimals: number;
  bump: number;
  sources: OracleBinding;
  /** The multiplier in force at the last sync, fixed-point 1e12. */
  observedMultiplier: bigint;
  /** A scheduled change, or zero when there is none pending. */
  pendingMultiplier: bigint;
  pendingEffectiveTs: bigint;
  /**
   * Increments whenever `observed_multiplier` moves, so an indexer can count
   * corporate actions without diffing every sync.
   */
  multiplierEpoch: bigint;
  mintPaused: boolean;
  transferHook: Option<Address>;
  primary: Observation;
  secondary: Option<Observation>;
  syncedTs: bigint;
  halt: HaltState;
  maxPriceAge: number;
  maxConfBps: number;
  maxDivergenceBps: number;
  /**
   * Counted by `probe_security`, which is the only path that can record a
   * refusal: an instruction that refuses reverts, so its own counter goes
   * with it. The refused transaction's logs carry the `Refused` event.
   */
  refusals: number;
  lastRefusalCode: number;
  lastRefusalTs: bigint;
};

export type SecurityStateArgs = {
  /** The Token-2022 mint this security *is*. Also its PDA seed. */
  underlyingMint: Address;
  /** ASCII, right-padded with zeroes: "AAPLx". */
  symbol: ReadonlyUint8Array;
  /** Which committed exchange calendar gates it. */
  calendarId: number;
  /** Copied from the mint so a reader does not have to unpack it. */
  decimals: number;
  bump: number;
  sources: OracleBindingArgs;
  /** The multiplier in force at the last sync, fixed-point 1e12. */
  observedMultiplier: number | bigint;
  /** A scheduled change, or zero when there is none pending. */
  pendingMultiplier: number | bigint;
  pendingEffectiveTs: number | bigint;
  /**
   * Increments whenever `observed_multiplier` moves, so an indexer can count
   * corporate actions without diffing every sync.
   */
  multiplierEpoch: number | bigint;
  mintPaused: boolean;
  transferHook: OptionOrNullable<Address>;
  primary: ObservationArgs;
  secondary: OptionOrNullable<ObservationArgs>;
  syncedTs: number | bigint;
  halt: HaltStateArgs;
  maxPriceAge: number;
  maxConfBps: number;
  maxDivergenceBps: number;
  /**
   * Counted by `probe_security`, which is the only path that can record a
   * refusal: an instruction that refuses reverts, so its own counter goes
   * with it. The refused transaction's logs carry the `Refused` event.
   */
  refusals: number;
  lastRefusalCode: number;
  lastRefusalTs: number | bigint;
};

/** Gets the encoder for {@link SecurityStateArgs} account data. */
export function getSecurityStateEncoder(): Encoder<SecurityStateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["underlyingMint", getAddressEncoder()],
      ["symbol", fixEncoderSize(getBytesEncoder(), 12)],
      ["calendarId", getU16Encoder()],
      ["decimals", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["sources", getOracleBindingEncoder()],
      ["observedMultiplier", getU128Encoder()],
      ["pendingMultiplier", getU128Encoder()],
      ["pendingEffectiveTs", getI64Encoder()],
      ["multiplierEpoch", getU64Encoder()],
      ["mintPaused", getBooleanEncoder()],
      ["transferHook", getOptionEncoder(getAddressEncoder())],
      ["primary", getObservationEncoder()],
      ["secondary", getOptionEncoder(getObservationEncoder())],
      ["syncedTs", getI64Encoder()],
      ["halt", getHaltStateEncoder()],
      ["maxPriceAge", getU32Encoder()],
      ["maxConfBps", getU32Encoder()],
      ["maxDivergenceBps", getU32Encoder()],
      ["refusals", getU32Encoder()],
      ["lastRefusalCode", getU8Encoder()],
      ["lastRefusalTs", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SECURITY_STATE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SecurityState} account data. */
export function getSecurityStateDecoder(): Decoder<SecurityState> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["underlyingMint", getAddressDecoder()],
    ["symbol", fixDecoderSize(getBytesDecoder(), 12)],
    ["calendarId", getU16Decoder()],
    ["decimals", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["sources", getOracleBindingDecoder()],
    ["observedMultiplier", getU128Decoder()],
    ["pendingMultiplier", getU128Decoder()],
    ["pendingEffectiveTs", getI64Decoder()],
    ["multiplierEpoch", getU64Decoder()],
    ["mintPaused", getBooleanDecoder()],
    ["transferHook", getOptionDecoder(getAddressDecoder())],
    ["primary", getObservationDecoder()],
    ["secondary", getOptionDecoder(getObservationDecoder())],
    ["syncedTs", getI64Decoder()],
    ["halt", getHaltStateDecoder()],
    ["maxPriceAge", getU32Decoder()],
    ["maxConfBps", getU32Decoder()],
    ["maxDivergenceBps", getU32Decoder()],
    ["refusals", getU32Decoder()],
    ["lastRefusalCode", getU8Decoder()],
    ["lastRefusalTs", getI64Decoder()],
  ]);
}

/** Gets the codec for {@link SecurityState} account data. */
export function getSecurityStateCodec(): Codec<
  SecurityStateArgs,
  SecurityState
> {
  return combineCodec(getSecurityStateEncoder(), getSecurityStateDecoder());
}

export function decodeSecurityState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SecurityState, TAddress>;
export function decodeSecurityState<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SecurityState, TAddress>;
export function decodeSecurityState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SecurityState, TAddress> | MaybeAccount<SecurityState, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSecurityStateDecoder(),
  );
}

export async function fetchSecurityState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SecurityState, TAddress>> {
  const maybeAccount = await fetchMaybeSecurityState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSecurityState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SecurityState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSecurityState(maybeAccount);
}

export async function fetchAllSecurityState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SecurityState>[]> {
  const maybeAccounts = await fetchAllMaybeSecurityState(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSecurityState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SecurityState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSecurityState(maybeAccount));
}
