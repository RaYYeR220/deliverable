/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getOptionKindDecoder,
  getOptionKindEncoder,
  type OptionKind,
  type OptionKindArgs,
} from "../types/index.js";

export const OPTION_SERIES_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  133, 216, 220, 240, 189, 120, 149, 116,
]);

export function getOptionSeriesDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    OPTION_SERIES_DISCRIMINATOR,
  );
}

export type OptionSeries = {
  discriminator: ReadonlyUint8Array;
  security: Address;
  underlyingMint: Address;
  quoteMint: Address;
  /** Minted 1:1 with contracts, zero decimals. Burned on exercise. */
  optionMint: Address;
  /** Holds the raw underlying that backs every written contract. */
  collateralVault: Address;
  /**
   * Premium, denominated in the underlying share. A covered call written
   * here accumulates the share itself.
   */
  premiumVault: Address;
  /** Exercise proceeds, in the quote asset. */
  quoteVault: Address;
  creator: Address;
  kind: OptionKind;
  /** Validated at creation to land inside a regular session. */
  expiryTs: bigint;
  /** Quote units per UI unit — per adjusted share — at creation. */
  strike0: bigint;
  /**
   * The mint's effective multiplier when the series was created. Immutable:
   * it is half of the invariant.
   */
  multiplierAtMint: bigint;
  /**
   * Raw units of the underlying one contract covers. Immutable, and raw
   * rather than UI precisely so a split cannot change what is delivered.
   */
  contractRawSize: bigint;
  /** Length of the exercise window, in minutes of open-market time. */
  settlementWindowMinutes: number;
  /**
   * False writes an unadjusted series: the strike ignores the mint. Every
   * options venue on tokenized equities today writes one of these by
   * omission. Being able to express it is what makes the invariant
   * falsifiable instead of merely asserted.
   */
  adjustOnCorporateAction: boolean;
  underlyingDecimals: number;
  quoteDecimals: number;
  bump: number;
  contractsWritten: bigint;
  contractsExercised: bigint;
  /** Quote units taken in by exercises, before writers claim them. */
  quoteCollected: bigint;
  /** Premium already paid out, so a later claimant sees the same denominator. */
  premiumClaimedTotal: bigint;
  /**
   * First instant after expiry at which the gate allowed an exercise. Zero
   * until one does; informational, the window itself is computed.
   */
  windowOpenedTs: bigint;
  /** The multiplier the last `acknowledge_adjustment` reported. */
  acknowledgedMultiplier: bigint;
  /**
   * Contracts already assigned to writers who have settled.
   *
   * Assignment has to sum to exactly `contracts_exercised`: pro rata over
   * integers rounds *every* writer down, so the writers collectively reclaim
   * more raw collateral than the vault holds and whoever settles after it
   * dries takes the loss. The running total is what makes the sum exact.
   */
  contractsAssignedTotal: bigint;
  /**
   * Premium accrued per contract since the series was listed, scaled by
   * [`SCALE`], monotonic.
   *
   * Premium is time-weighted rather than split by the book as it stands at
   * claim time: an accumulator that only ever moves forward, credited before
   * `contracts_written` changes, means a writer can claim exactly the
   * premium that arrived while their contracts were outstanding and nothing
   * else. Splitting by the instantaneous book let a writer who arrived after
   * the premium did take a share of it, in the same transaction that opened
   * their position.
   */
  premiumPerContractAcc: bigint;
  /**
   * Premium already folded into the accumulator. The difference between this
   * and the live pool is what the next accrual credits, so the remainder
   * integer division leaves behind rolls forward instead of stranding.
   */
  premiumCreditedTotal: bigint;
};

export type OptionSeriesArgs = {
  security: Address;
  underlyingMint: Address;
  quoteMint: Address;
  /** Minted 1:1 with contracts, zero decimals. Burned on exercise. */
  optionMint: Address;
  /** Holds the raw underlying that backs every written contract. */
  collateralVault: Address;
  /**
   * Premium, denominated in the underlying share. A covered call written
   * here accumulates the share itself.
   */
  premiumVault: Address;
  /** Exercise proceeds, in the quote asset. */
  quoteVault: Address;
  creator: Address;
  kind: OptionKindArgs;
  /** Validated at creation to land inside a regular session. */
  expiryTs: number | bigint;
  /** Quote units per UI unit — per adjusted share — at creation. */
  strike0: number | bigint;
  /**
   * The mint's effective multiplier when the series was created. Immutable:
   * it is half of the invariant.
   */
  multiplierAtMint: number | bigint;
  /**
   * Raw units of the underlying one contract covers. Immutable, and raw
   * rather than UI precisely so a split cannot change what is delivered.
   */
  contractRawSize: number | bigint;
  /** Length of the exercise window, in minutes of open-market time. */
  settlementWindowMinutes: number;
  /**
   * False writes an unadjusted series: the strike ignores the mint. Every
   * options venue on tokenized equities today writes one of these by
   * omission. Being able to express it is what makes the invariant
   * falsifiable instead of merely asserted.
   */
  adjustOnCorporateAction: boolean;
  underlyingDecimals: number;
  quoteDecimals: number;
  bump: number;
  contractsWritten: number | bigint;
  contractsExercised: number | bigint;
  /** Quote units taken in by exercises, before writers claim them. */
  quoteCollected: number | bigint;
  /** Premium already paid out, so a later claimant sees the same denominator. */
  premiumClaimedTotal: number | bigint;
  /**
   * First instant after expiry at which the gate allowed an exercise. Zero
   * until one does; informational, the window itself is computed.
   */
  windowOpenedTs: number | bigint;
  /** The multiplier the last `acknowledge_adjustment` reported. */
  acknowledgedMultiplier: number | bigint;
  /**
   * Contracts already assigned to writers who have settled.
   *
   * Assignment has to sum to exactly `contracts_exercised`: pro rata over
   * integers rounds *every* writer down, so the writers collectively reclaim
   * more raw collateral than the vault holds and whoever settles after it
   * dries takes the loss. The running total is what makes the sum exact.
   */
  contractsAssignedTotal: number | bigint;
  /**
   * Premium accrued per contract since the series was listed, scaled by
   * [`SCALE`], monotonic.
   *
   * Premium is time-weighted rather than split by the book as it stands at
   * claim time: an accumulator that only ever moves forward, credited before
   * `contracts_written` changes, means a writer can claim exactly the
   * premium that arrived while their contracts were outstanding and nothing
   * else. Splitting by the instantaneous book let a writer who arrived after
   * the premium did take a share of it, in the same transaction that opened
   * their position.
   */
  premiumPerContractAcc: number | bigint;
  /**
   * Premium already folded into the accumulator. The difference between this
   * and the live pool is what the next accrual credits, so the remainder
   * integer division leaves behind rolls forward instead of stranding.
   */
  premiumCreditedTotal: number | bigint;
};

/** Gets the encoder for {@link OptionSeriesArgs} account data. */
export function getOptionSeriesEncoder(): FixedSizeEncoder<OptionSeriesArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["security", getAddressEncoder()],
      ["underlyingMint", getAddressEncoder()],
      ["quoteMint", getAddressEncoder()],
      ["optionMint", getAddressEncoder()],
      ["collateralVault", getAddressEncoder()],
      ["premiumVault", getAddressEncoder()],
      ["quoteVault", getAddressEncoder()],
      ["creator", getAddressEncoder()],
      ["kind", getOptionKindEncoder()],
      ["expiryTs", getI64Encoder()],
      ["strike0", getU64Encoder()],
      ["multiplierAtMint", getU128Encoder()],
      ["contractRawSize", getU64Encoder()],
      ["settlementWindowMinutes", getU16Encoder()],
      ["adjustOnCorporateAction", getBooleanEncoder()],
      ["underlyingDecimals", getU8Encoder()],
      ["quoteDecimals", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["contractsWritten", getU64Encoder()],
      ["contractsExercised", getU64Encoder()],
      ["quoteCollected", getU64Encoder()],
      ["premiumClaimedTotal", getU64Encoder()],
      ["windowOpenedTs", getI64Encoder()],
      ["acknowledgedMultiplier", getU128Encoder()],
      ["contractsAssignedTotal", getU64Encoder()],
      ["premiumPerContractAcc", getU128Encoder()],
      ["premiumCreditedTotal", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: OPTION_SERIES_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link OptionSeries} account data. */
export function getOptionSeriesDecoder(): FixedSizeDecoder<OptionSeries> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["security", getAddressDecoder()],
    ["underlyingMint", getAddressDecoder()],
    ["quoteMint", getAddressDecoder()],
    ["optionMint", getAddressDecoder()],
    ["collateralVault", getAddressDecoder()],
    ["premiumVault", getAddressDecoder()],
    ["quoteVault", getAddressDecoder()],
    ["creator", getAddressDecoder()],
    ["kind", getOptionKindDecoder()],
    ["expiryTs", getI64Decoder()],
    ["strike0", getU64Decoder()],
    ["multiplierAtMint", getU128Decoder()],
    ["contractRawSize", getU64Decoder()],
    ["settlementWindowMinutes", getU16Decoder()],
    ["adjustOnCorporateAction", getBooleanDecoder()],
    ["underlyingDecimals", getU8Decoder()],
    ["quoteDecimals", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["contractsWritten", getU64Decoder()],
    ["contractsExercised", getU64Decoder()],
    ["quoteCollected", getU64Decoder()],
    ["premiumClaimedTotal", getU64Decoder()],
    ["windowOpenedTs", getI64Decoder()],
    ["acknowledgedMultiplier", getU128Decoder()],
    ["contractsAssignedTotal", getU64Decoder()],
    ["premiumPerContractAcc", getU128Decoder()],
    ["premiumCreditedTotal", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link OptionSeries} account data. */
export function getOptionSeriesCodec(): FixedSizeCodec<
  OptionSeriesArgs,
  OptionSeries
> {
  return combineCodec(getOptionSeriesEncoder(), getOptionSeriesDecoder());
}

export function decodeOptionSeries<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<OptionSeries, TAddress>;
export function decodeOptionSeries<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<OptionSeries, TAddress>;
export function decodeOptionSeries<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<OptionSeries, TAddress> | MaybeAccount<OptionSeries, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getOptionSeriesDecoder(),
  );
}

export async function fetchOptionSeries<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<OptionSeries, TAddress>> {
  const maybeAccount = await fetchMaybeOptionSeries(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeOptionSeries<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<OptionSeries, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeOptionSeries(maybeAccount);
}

export async function fetchAllOptionSeries(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<OptionSeries>[]> {
  const maybeAccounts = await fetchAllMaybeOptionSeries(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeOptionSeries(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<OptionSeries>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeOptionSeries(maybeAccount));
}

export function getOptionSeriesSize(): number {
  return 399;
}
