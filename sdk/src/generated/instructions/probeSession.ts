/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { DELIVERABLE_PROGRAM_ADDRESS } from "../programs/index.js";

export const PROBE_SESSION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  31, 120, 167, 116, 205, 156, 135, 167,
]);

export function getProbeSessionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROBE_SESSION_DISCRIMINATOR,
  );
}

export type ProbeSessionInstruction<
  TProgram extends string = typeof DELIVERABLE_PROGRAM_ADDRESS,
  TAccountCalendar extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCalendar extends string
        ? ReadonlyAccount<TAccountCalendar>
        : TAccountCalendar,
      ...TRemainingAccounts,
    ]
  >;

export type ProbeSessionInstructionData = { discriminator: ReadonlyUint8Array };

export type ProbeSessionInstructionDataArgs = {};

export function getProbeSessionInstructionDataEncoder(): FixedSizeEncoder<ProbeSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PROBE_SESSION_DISCRIMINATOR }),
  );
}

export function getProbeSessionInstructionDataDecoder(): FixedSizeDecoder<ProbeSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getProbeSessionInstructionDataCodec(): FixedSizeCodec<
  ProbeSessionInstructionDataArgs,
  ProbeSessionInstructionData
> {
  return combineCodec(
    getProbeSessionInstructionDataEncoder(),
    getProbeSessionInstructionDataDecoder(),
  );
}

export type ProbeSessionInput<
  TAccountCalendar extends InstructionAccountInput = InstructionAccountInput,
> = {
  calendar: TAccountCalendar;
};

export function getProbeSessionInstruction<
  TAccountCalendar extends InstructionAccountInput,
  TProgramAddress extends Address = typeof DELIVERABLE_PROGRAM_ADDRESS,
>(
  input: ProbeSessionInput<TAccountCalendar>,
  config?: { programAddress?: TProgramAddress },
): ProbeSessionInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountCalendar,
    InstructionAccountInputAddress<TAccountCalendar>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DELIVERABLE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    calendar: {
      value: input.calendar ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [getAccountMeta("calendar", accounts.calendar)],
    data: getProbeSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as ProbeSessionInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountCalendar,
      InstructionAccountInputAddress<TAccountCalendar>
    >
  >);
}

export type ParsedProbeSessionInstruction<
  TProgram extends string = typeof DELIVERABLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    calendar: TAccountMetas[0];
  };
  data: ProbeSessionInstructionData;
};

export function parseProbeSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProbeSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { calendar: getNextAccount() },
    data: getProbeSessionInstructionDataDecoder().decode(instruction.data),
  };
}
