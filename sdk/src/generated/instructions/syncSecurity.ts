/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findSecurityPda } from "../pdas/index.js";
import { DELIVERABLE_PROGRAM_ADDRESS } from "../programs/index.js";

export const SYNC_SECURITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  45, 127, 218, 31, 124, 175, 28, 215,
]);

export function getSyncSecurityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SYNC_SECURITY_DISCRIMINATOR,
  );
}

export type SyncSecurityInstruction<
  TProgram extends string = typeof DELIVERABLE_PROGRAM_ADDRESS,
  TAccountSecurity extends string | AccountMeta<string> = string,
  TAccountUnderlyingMint extends string | AccountMeta<string> = string,
  TAccountPrimaryOracle extends string | AccountMeta<string> = string,
  TAccountSecondaryOracle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSecurity extends string
        ? WritableAccount<TAccountSecurity>
        : TAccountSecurity,
      TAccountUnderlyingMint extends string
        ? ReadonlyAccount<TAccountUnderlyingMint>
        : TAccountUnderlyingMint,
      TAccountPrimaryOracle extends string
        ? ReadonlyAccount<TAccountPrimaryOracle>
        : TAccountPrimaryOracle,
      TAccountSecondaryOracle extends string
        ? ReadonlyAccount<TAccountSecondaryOracle>
        : TAccountSecondaryOracle,
      ...TRemainingAccounts,
    ]
  >;

export type SyncSecurityInstructionData = { discriminator: ReadonlyUint8Array };

export type SyncSecurityInstructionDataArgs = {};

export function getSyncSecurityInstructionDataEncoder(): FixedSizeEncoder<SyncSecurityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SYNC_SECURITY_DISCRIMINATOR }),
  );
}

export function getSyncSecurityInstructionDataDecoder(): FixedSizeDecoder<SyncSecurityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSyncSecurityInstructionDataCodec(): FixedSizeCodec<
  SyncSecurityInstructionDataArgs,
  SyncSecurityInstructionData
> {
  return combineCodec(
    getSyncSecurityInstructionDataEncoder(),
    getSyncSecurityInstructionDataDecoder(),
  );
}

export type SyncSecurityAsyncInput<
  TAccountSecurity extends InstructionAccountInput = InstructionAccountInput,
  TAccountUnderlyingMint extends InstructionAccountInput =
    InstructionAccountInput,
  TAccountPrimaryOracle extends InstructionAccountInput =
    InstructionAccountInput,
  TAccountSecondaryOracle extends InstructionAccountInput =
    InstructionAccountInput,
> = {
  security?: TAccountSecurity;
  underlyingMint: TAccountUnderlyingMint;
  /**
   * registered with, which checks the account's **address**, its owning
   * program, its discriminator and its length before reading a byte of it.
   * Unchecked here and checked there because the adapter is chosen by the
   * registration, not by the account list.
   */
  primaryOracle: TAccountPrimaryOracle;
  /**
   * with a single declared source — it is never read in that case, and the
   * gate refuses regardless.
   */
  secondaryOracle: TAccountSecondaryOracle;
};

export async function getSyncSecurityInstructionAsync<
  TAccountSecurity extends InstructionAccountInput,
  TAccountUnderlyingMint extends InstructionAccountInput,
  TAccountPrimaryOracle extends InstructionAccountInput,
  TAccountSecondaryOracle extends InstructionAccountInput,
  TProgramAddress extends Address = typeof DELIVERABLE_PROGRAM_ADDRESS,
>(
  input: SyncSecurityAsyncInput<
    TAccountSecurity,
    TAccountUnderlyingMint,
    TAccountPrimaryOracle,
    TAccountSecondaryOracle
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SyncSecurityInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountSecurity,
      InstructionAccountInputAddress<TAccountSecurity>
    >,
    ResolvedInstructionAccountMeta<
      TAccountUnderlyingMint,
      InstructionAccountInputAddress<TAccountUnderlyingMint>
    >,
    ResolvedInstructionAccountMeta<
      TAccountPrimaryOracle,
      InstructionAccountInputAddress<TAccountPrimaryOracle>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSecondaryOracle,
      InstructionAccountInputAddress<TAccountSecondaryOracle>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DELIVERABLE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    security: {
      value: input.security ?? null,
      isSigner: false,
      isWritable: true,
    },
    underlyingMint: {
      value: input.underlyingMint ?? null,
      isSigner: false,
      isWritable: false,
    },
    primaryOracle: {
      value: input.primaryOracle ?? null,
      isSigner: false,
      isWritable: false,
    },
    secondaryOracle: {
      value: input.secondaryOracle ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.security.value) {
    accounts.security.value = await findSecurityPda(
      {
        underlyingMint: getAddressFromResolvedInstructionAccount(
          "underlyingMint",
          accounts.underlyingMint.value,
        ),
      },
      { programAddress },
    );
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("security", accounts.security),
      getAccountMeta("underlyingMint", accounts.underlyingMint),
      getAccountMeta("primaryOracle", accounts.primaryOracle),
      getAccountMeta("secondaryOracle", accounts.secondaryOracle),
    ],
    data: getSyncSecurityInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncSecurityInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountSecurity,
      InstructionAccountInputAddress<TAccountSecurity>
    >,
    ResolvedInstructionAccountMeta<
      TAccountUnderlyingMint,
      InstructionAccountInputAddress<TAccountUnderlyingMint>
    >,
    ResolvedInstructionAccountMeta<
      TAccountPrimaryOracle,
      InstructionAccountInputAddress<TAccountPrimaryOracle>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSecondaryOracle,
      InstructionAccountInputAddress<TAccountSecondaryOracle>
    >
  >);
}

export type SyncSecurityInput<
  TAccountSecurity extends InstructionAccountInput = InstructionAccountInput,
  TAccountUnderlyingMint extends InstructionAccountInput =
    InstructionAccountInput,
  TAccountPrimaryOracle extends InstructionAccountInput =
    InstructionAccountInput,
  TAccountSecondaryOracle extends InstructionAccountInput =
    InstructionAccountInput,
> = {
  security: TAccountSecurity;
  underlyingMint: TAccountUnderlyingMint;
  /**
   * registered with, which checks the account's **address**, its owning
   * program, its discriminator and its length before reading a byte of it.
   * Unchecked here and checked there because the adapter is chosen by the
   * registration, not by the account list.
   */
  primaryOracle: TAccountPrimaryOracle;
  /**
   * with a single declared source — it is never read in that case, and the
   * gate refuses regardless.
   */
  secondaryOracle: TAccountSecondaryOracle;
};

export function getSyncSecurityInstruction<
  TAccountSecurity extends InstructionAccountInput,
  TAccountUnderlyingMint extends InstructionAccountInput,
  TAccountPrimaryOracle extends InstructionAccountInput,
  TAccountSecondaryOracle extends InstructionAccountInput,
  TProgramAddress extends Address = typeof DELIVERABLE_PROGRAM_ADDRESS,
>(
  input: SyncSecurityInput<
    TAccountSecurity,
    TAccountUnderlyingMint,
    TAccountPrimaryOracle,
    TAccountSecondaryOracle
  >,
  config?: { programAddress?: TProgramAddress },
): SyncSecurityInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountSecurity,
    InstructionAccountInputAddress<TAccountSecurity>
  >,
  ResolvedInstructionAccountMeta<
    TAccountUnderlyingMint,
    InstructionAccountInputAddress<TAccountUnderlyingMint>
  >,
  ResolvedInstructionAccountMeta<
    TAccountPrimaryOracle,
    InstructionAccountInputAddress<TAccountPrimaryOracle>
  >,
  ResolvedInstructionAccountMeta<
    TAccountSecondaryOracle,
    InstructionAccountInputAddress<TAccountSecondaryOracle>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DELIVERABLE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    security: {
      value: input.security ?? null,
      isSigner: false,
      isWritable: true,
    },
    underlyingMint: {
      value: input.underlyingMint ?? null,
      isSigner: false,
      isWritable: false,
    },
    primaryOracle: {
      value: input.primaryOracle ?? null,
      isSigner: false,
      isWritable: false,
    },
    secondaryOracle: {
      value: input.secondaryOracle ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [
      getAccountMeta("security", accounts.security),
      getAccountMeta("underlyingMint", accounts.underlyingMint),
      getAccountMeta("primaryOracle", accounts.primaryOracle),
      getAccountMeta("secondaryOracle", accounts.secondaryOracle),
    ],
    data: getSyncSecurityInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncSecurityInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountSecurity,
      InstructionAccountInputAddress<TAccountSecurity>
    >,
    ResolvedInstructionAccountMeta<
      TAccountUnderlyingMint,
      InstructionAccountInputAddress<TAccountUnderlyingMint>
    >,
    ResolvedInstructionAccountMeta<
      TAccountPrimaryOracle,
      InstructionAccountInputAddress<TAccountPrimaryOracle>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSecondaryOracle,
      InstructionAccountInputAddress<TAccountSecondaryOracle>
    >
  >);
}

export type ParsedSyncSecurityInstruction<
  TProgram extends string = typeof DELIVERABLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    security: TAccountMetas[0];
    underlyingMint: TAccountMetas[1];
    /**
     * registered with, which checks the account's **address**, its owning
     * program, its discriminator and its length before reading a byte of it.
     * Unchecked here and checked there because the adapter is chosen by the
     * registration, not by the account list.
     */
    primaryOracle: TAccountMetas[2];
    /**
     * with a single declared source — it is never read in that case, and the
     * gate refuses regardless.
     */
    secondaryOracle: TAccountMetas[3];
  };
  data: SyncSecurityInstructionData;
};

export function parseSyncSecurityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSyncSecurityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      security: getNextAccount(),
      underlyingMint: getNextAccount(),
      primaryOracle: getNextAccount(),
      secondaryOracle: getNextAccount(),
    },
    data: getSyncSecurityInstructionDataDecoder().decode(instruction.data),
  };
}
