/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getBooleanDecoder,
  getBooleanEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Attested halt state for a security.
 *
 * A halt is the one condition no on-chain source can tell us: `PriceUpdateV2`
 * dropped the legacy `PriceStatus` enum, so halted, closed, outage and early
 * close are indistinguishable from staleness alone. It therefore arrives
 * signed by the registry's attestor and is stored, not inferred.
 */
export type HaltState = {
  halted: boolean;
  /** When the halt began, as reported by the source. */
  sinceTs: bigint;
  /**
   * When the attestor signed it. Stale attestations are the attestor's
   * problem to refresh, not something we silently expire.
   */
  attestedTs: bigint;
  /** Which feed the attestation came from. */
  source: number;
};

export type HaltStateArgs = {
  halted: boolean;
  /** When the halt began, as reported by the source. */
  sinceTs: number | bigint;
  /**
   * When the attestor signed it. Stale attestations are the attestor's
   * problem to refresh, not something we silently expire.
   */
  attestedTs: number | bigint;
  /** Which feed the attestation came from. */
  source: number;
};

export function getHaltStateEncoder(): FixedSizeEncoder<HaltStateArgs> {
  return getStructEncoder([
    ["halted", getBooleanEncoder()],
    ["sinceTs", getI64Encoder()],
    ["attestedTs", getI64Encoder()],
    ["source", getU8Encoder()],
  ]);
}

export function getHaltStateDecoder(): FixedSizeDecoder<HaltState> {
  return getStructDecoder([
    ["halted", getBooleanDecoder()],
    ["sinceTs", getI64Decoder()],
    ["attestedTs", getI64Decoder()],
    ["source", getU8Decoder()],
  ]);
}

export function getHaltStateCodec(): FixedSizeCodec<HaltStateArgs, HaltState> {
  return combineCodec(getHaltStateEncoder(), getHaltStateDecoder());
}
