/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const POSITION_SETTLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([75, 100, 92, 189, 245, 116, 252, 221]);

export function getPositionSettledEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    POSITION_SETTLED_EVENT_DISCRIMINATOR,
  );
}

export type PositionSettledEvent = {
  series: Address;
  writer: Address;
  contractsAssigned: bigint;
  rawReturned: bigint;
  quotePaid: bigint;
  /**
   * Raw units this writer was owed and the vault could not pay.
   *
   * Zero for every ordinary settlement: assignment conserves, so the vault
   * holds exactly what the unassigned writers are owed. It can only be
   * non-zero if the underlying left the vault by a route this program does
   * not control — a permanent-delegate seizure — and in that case it must be
   * on the wire rather than absorbed silently by whoever settles last.
   */
  rawShortfall: bigint;
};

export type PositionSettledEventArgs = {
  series: Address;
  writer: Address;
  contractsAssigned: number | bigint;
  rawReturned: number | bigint;
  quotePaid: number | bigint;
  /**
   * Raw units this writer was owed and the vault could not pay.
   *
   * Zero for every ordinary settlement: assignment conserves, so the vault
   * holds exactly what the unassigned writers are owed. It can only be
   * non-zero if the underlying left the vault by a route this program does
   * not control — a permanent-delegate seizure — and in that case it must be
   * on the wire rather than absorbed silently by whoever settles last.
   */
  rawShortfall: number | bigint;
};

/** Gets the encoder for {@link PositionSettledEventArgs} event data. */
export function getPositionSettledEventEncoder(): FixedSizeEncoder<PositionSettledEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["series", getAddressEncoder()],
      ["writer", getAddressEncoder()],
      ["contractsAssigned", getU64Encoder()],
      ["rawReturned", getU64Encoder()],
      ["quotePaid", getU64Encoder()],
      ["rawShortfall", getU64Encoder()],
    ]),
    [getConstantEncoder(POSITION_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link PositionSettledEvent} event data. */
export function getPositionSettledEventDecoder(): FixedSizeDecoder<PositionSettledEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["series", getAddressDecoder()],
      ["writer", getAddressDecoder()],
      ["contractsAssigned", getU64Decoder()],
      ["rawReturned", getU64Decoder()],
      ["quotePaid", getU64Decoder()],
      ["rawShortfall", getU64Decoder()],
    ]),
    [getConstantDecoder(POSITION_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link PositionSettledEvent} event data. */
export function getPositionSettledEventCodec(): FixedSizeCodec<
  PositionSettledEventArgs,
  PositionSettledEvent
> {
  return combineCodec(
    getPositionSettledEventEncoder(),
    getPositionSettledEventDecoder(),
  );
}

/** Parses the provided bytes into {@link PositionSettledEvent} event data. */
export function parsePositionSettledEvent(
  data: ReadonlyUint8Array | Uint8Array,
): PositionSettledEvent {
  if (containsBytes(data, POSITION_SETTLED_EVENT_DISCRIMINATOR, 0)) {
    return getPositionSettledEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "PositionSettledEvent" event discriminators.',
  );
}
