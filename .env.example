# Pyth Hermes — https://pythdata.app/signup (free trial). Required for price updates;
# the /v2/price_feeds metadata endpoint works without it.
PYTH_API_KEY=
PYTH_HERMES_URL=https://hermes.pyth.network

# Solana RPC. The public mainnet-beta endpoint blocks getProgramAccounts.
SOLANA_RPC_URL=
HELIUS_API_KEY=

# Clawpump agent API key (cpk_...)
CLAWPUMP_API_KEY=
